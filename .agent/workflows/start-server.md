---
description: Start the local development server for Goodplanr
---

// turbo
1. Run the development server:
```bash
export PATH="/Users/aggole/.nvm/versions/node/v24.11.1/bin:$PATH" && npm run dev
```

The server will start on http://localhost:3000
