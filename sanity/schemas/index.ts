import product from './product';
import banner from './banner';
import testimonial from './testimonial';

import review from './review';

export const schema = {
    types: [product, banner, testimonial, review],
};
